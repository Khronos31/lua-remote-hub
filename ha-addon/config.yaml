name: "Lua_Remote_Hub (LRH)"
description: "Luaベースのリモコン司令部"
version: "1.0.0"
slug: "lua-remote-hub"
arch:
  - aarch64
  - amd64
  - armv7
init: false

# 権限設定
homeassistant_api: true
hassio_api: true
hassio_role: admin

# ネットワーク設定
host_network: true

# 不要な options と schema の不一致を解消（空にするか削除）
options: {}
schema: {}

map:
  - addon_config:rw